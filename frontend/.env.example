# Content Atlas Frontend Configuration

# API URL - Update this to point to your FastAPI backend
VITE_API_URL=http://localhost:8000

# For production deployment, use your actual API URL:
# VITE_API_URL=https://api.yourdomain.com

# Upload Configuration
VITE_MAX_UPLOAD_SIZE_MB=100

# Upload mode: 'direct' for browser-to-B2 uploads, 'proxied' for uploads through backend
# Use 'proxied' for testing/development to avoid CORS issues
VITE_UPLOAD_MODE=proxied
